<UserControl x:Class="DanteCLI.Views.FavoritesSidebar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Margin="12,8,12,4">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock Text="FAVORITOS" FontSize="11" FontWeight="SemiBold" Opacity="0.6" />
                <Button Grid.Column="1" Background="Transparent" BorderThickness="0"
                        Click="AddButton_Click" ToolTip="Adicionar favorito">
                    <TextBlock Text="+" FontSize="14" />
                </Button>
            </Grid>
            <TextBox x:Name="SearchBox" Margin="0,6,0,0" Padding="6,4"
                     TextChanged="SearchBox_TextChanged">
                <TextBox.Style>
                    <Style TargetType="TextBox">
                        <Style.Resources>
                            <VisualBrush x:Key="HintBrush" AlignmentX="Left" AlignmentY="Center" Stretch="None">
                                <VisualBrush.Visual>
                                    <TextBlock Text="🔍 Buscar..." Foreground="#999" Padding="4,0"/>
                                </VisualBrush.Visual>
                            </VisualBrush>
                        </Style.Resources>
                        <Style.Triggers>
                            <Trigger Property="Text" Value="">
                                <Setter Property="Background" Value="{StaticResource HintBrush}" />
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter Property="Background" Value="White" />
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </TextBox.Style>
            </TextBox>
        </StackPanel>
        <ListBox x:Name="List" Grid.Row="1" BorderThickness="0" Background="Transparent"
                 SelectionMode="Single" Margin="6,0,6,6"
                 MouseDoubleClick="List_MouseDoubleClick">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Margin="0,3">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding Emoji}" FontSize="13" Margin="0,0,6,0" />
                            <TextBlock Text="{Binding Name}" FontSize="12" FontWeight="Medium" />
                        </StackPanel>
                        <TextBlock Text="{Binding DisplayPath}" FontSize="10" Opacity="0.6"
                                   TextTrimming="CharacterEllipsis" />
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>
