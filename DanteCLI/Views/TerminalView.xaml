<UserControl x:Class="DanteCLI.Views.TerminalView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#1A1B26" Focusable="True">
    <Grid Margin="10">
        <ScrollViewer x:Name="Scroller" VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Hidden" Background="Transparent"
                      PreviewMouseWheel="Scroller_PreviewMouseWheel">
            <RichTextBox x:Name="OutputView" Background="Transparent" BorderThickness="0"
                         Foreground="#C0CAF5" IsReadOnly="True"
                         FontFamily="Cascadia Mono, Consolas, Courier New" FontSize="13"
                         Padding="0" />
        </ScrollViewer>
        <Border x:Name="InputCapture" Background="Transparent" Focusable="True"
                IsHitTestVisible="True" PreviewKeyDown="InputCapture_PreviewKeyDown"
                PreviewTextInput="InputCapture_PreviewTextInput" />
    </Grid>
</UserControl>
