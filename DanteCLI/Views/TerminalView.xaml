<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="DanteCLI.Views.TerminalView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Background="#1A1B26">
    <Grid Padding="10">
        <ScrollViewer x:Name="Scroller"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Hidden"
                      Background="Transparent">
            <RichTextBlock x:Name="OutputView"
                           FontFamily="Cascadia Mono, Consolas, Courier New"
                           FontSize="13"
                           Foreground="#C0CAF5"
                           IsTextSelectionEnabled="True">
                <Paragraph x:Name="OutputParagraph" />
            </RichTextBlock>
        </ScrollViewer>

        <TextBox x:Name="InputCapture"
                 Background="Transparent"
                 BorderThickness="0"
                 Foreground="Transparent"
                 Opacity="0"
                 IsTabStop="True"
                 KeyDown="InputCapture_KeyDown"
                 BeforeTextChanging="InputCapture_BeforeTextChanging" />
    </Grid>
</UserControl>
