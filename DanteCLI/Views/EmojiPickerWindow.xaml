<Window x:Class="DanteCLI.Views.EmojiPickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Escolher emoji" Height="460" Width="420"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,8">
            <TextBlock DockPanel.Dock="Left" Text="🔍" VerticalAlignment="Center"
                       Margin="0,0,4,0" />
            <TextBox x:Name="SearchBox" Padding="6,4"
                     TextChanged="SearchBox_TextChanged" />
        </DockPanel>

        <ScrollViewer Grid.Row="1" Margin="0,0,0,8" HorizontalScrollBarVisibility="Auto"
                      VerticalScrollBarVisibility="Disabled">
            <ItemsControl x:Name="CategoriesList">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>
        </ScrollViewer>

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="EmojiGrid">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>
        </ScrollViewer>

        <DockPanel Grid.Row="3" Margin="0,8,0,0" LastChildFill="False">
            <Button DockPanel.Dock="Left" Content="Limpar emoji" Padding="10,4"
                    Click="Clear_Click" Background="Transparent" BorderThickness="0"
                    Foreground="#FF453A" />
            <Button DockPanel.Dock="Right" Content="Fechar" Padding="14,6"
                    IsCancel="True" Click="Cancel_Click" />
        </DockPanel>
    </Grid>
</Window>
