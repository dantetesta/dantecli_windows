<Window x:Class="DanteCLI.Views.TextInputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Height="160" Width="380"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock x:Name="Prompt" Grid.Row="0" Margin="0,0,0,8" />
        <TextBox x:Name="Input" Grid.Row="1" Padding="6,4" />
        <DockPanel Grid.Row="3" LastChildFill="False" Margin="0,16,0,0">
            <Button DockPanel.Dock="Right" Content="OK" Padding="14,6" Margin="6,0,0,0"
                    Click="Ok_Click" IsDefault="True" />
            <Button DockPanel.Dock="Right" Content="Cancelar" Padding="14,6"
                    IsCancel="True" Click="Cancel_Click" />
        </DockPanel>
    </Grid>
</Window>
